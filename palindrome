#include<stdio.h>
int main()
{
	int n,r,on,rev=0;
	scanf("%d",&n);
	on=n;
	while(n>0)
	{
		r=n%10;
		rev=rev*10+r;
		n=n/10;
	}
	if(rev==on)
	{
		printf("PALINDROME\n");
	}
	else
	{
		printf("NOT A PALINDROME");
	}
	return 0;
}
